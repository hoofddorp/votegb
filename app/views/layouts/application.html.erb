<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Athletes" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "App_Name" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <div id="login_nav"
          <%= render 'layouts/navigation' %>
          </div>
          <div id="general_nav"
               			<ul id="list-nav" class="navigation">
				<li> <%= link_to "Athletes", athletes_path %></li>
				<li> <%= link_to "Users",  %></li>
				<li> <a href="http://www.teamgb.com/" target = "_blank" >Team GB</a></li>
                                </ul>
          </div>     
        </div>
      </nav>
    </header>
    <div id="main" role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
              <p class="notice"><%= notice %></p>
              <p class="alert"><%= alert %></p>
          </div>
          <footer>
          </footer>
        </div>
      </div> 
    </div> 
  </body>
</html>
